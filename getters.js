//计算过滤操作，方便组件访问
export default {
    // startEndDate: state => {
    //     if (!state.startDate || !state.endDate) {
    //         return '起始日期-终止日期'
    //     }
    //     return `${state.startDate}-${state.endDate}`
    // },
    // curEmpCode:state=>{
    //     return state.curEmpInfo ? state.curEmpInfo.EmpCode : ""
    // },
    // curEmpName:state=>{
    //     return state.curEmpInfo ? state.curEmpInfo.EmpName : ""
    // }
}